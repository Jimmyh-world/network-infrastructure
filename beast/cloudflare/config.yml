# Cloudflare Tunnel Configuration
# This configuration file defines the routes for the beast-tunnel
# Generated from workflow Phase 5, Step 5.2

tunnel: beast-tunnel
credentials-file: /home/jimmyb/.cloudflared/beast-tunnel.json

ingress:
  # Grafana - Monitoring Dashboards
  - hostname: grafana.kitt.agency
    service: http://localhost:3000

  # Portainer - Container Management
  - hostname: portainer.kitt.agency
    service: https://localhost:9443
    originRequest:
      insecureSkipVerify: true

  # Ydun Scraper - Article extraction microservice
  - hostname: scrape.kitt.agency
    service: http://ydun-scraper:8080

  # Catch-all - reject any other traffic
  - service: http_status:404
